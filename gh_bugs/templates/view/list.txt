{%- if id_len == 1 -%}
{% set id_format = "%2d" %}
{%- else -%}
{% set id_format = "%%%dd" | format(id_len - 1) %}
{%- endif -%}
{{ "Id" | colourise("green") }} {{ spacer }}{{ "Title" | colourise("green") }}
{%- for bug in bugs %}
{{ id_format | format(bug.number) | colourise("yellow") }} {{ bug.title | truncate(max_title, True) }}
{%- endfor %}
